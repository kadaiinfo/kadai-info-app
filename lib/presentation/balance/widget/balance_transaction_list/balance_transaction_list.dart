import 'package:flutter/material.dart';

/// 取引一覧
class BalanceTransactionList extends StatelessWidget {
  const BalanceTransactionList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
