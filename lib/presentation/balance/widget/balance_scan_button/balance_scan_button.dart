import 'package:flutter/material.dart';

class BalanceScanButton extends StatelessWidget {
  const BalanceScanButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
