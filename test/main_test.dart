import 'dart:typed_data';

void main() {
  // final Uint8List idm = Uint8List.fromList([1, 46, 72, 199, 253, 195, 70, 48]);
  // final Uint8List pmm = Uint8List.fromList([3, 50, 66, 130, 130, 71, 170, 255]);
  // final maxTransceiveLength = 255;
  // final Uint8List systemCode = Uint8List.fromList([131, 101]);
  // final timeout = 255;
  // final a = Uint8List.view(idm.buffer);
  // print(idm.map((e) => e.toRadixString(16)));
}
