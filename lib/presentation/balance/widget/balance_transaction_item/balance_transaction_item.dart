import 'package:flutter/material.dart';

/// 取引
class BalanceTransactionItem extends StatelessWidget {
  const BalanceTransactionItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
